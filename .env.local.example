# Resend API Configuration
# Get your API key from https://resend.com/api-keys
RESEND_API_KEY=your_resend_api_key_here

# Make sure to:
# 1. Copy this file to .env.local (or set these in your deployment environment)
# 2. Verify that "no-reply@renderlab.ai" domain is verified in Resend dashboard
# 3. Test the email delivery after setting the API key
